diff --git a/node_modules/react-native-vision-camera-face-detector/android/src/main/java/com/margelo/nitro/camera/facedetector/HybridImageFaceDetector.kt b/node_modules/react-native-vision-camera-face-detector/android/src/main/java/com/margelo/nitro/camera/facedetector/HybridImageFaceDetector.kt
index 5d116a9..38efdcb 100644
--- a/node_modules/react-native-vision-camera-face-detector/android/src/main/java/com/margelo/nitro/camera/facedetector/HybridImageFaceDetector.kt
+++ b/node_modules/react-native-vision-camera-face-detector/android/src/main/java/com/margelo/nitro/camera/facedetector/HybridImageFaceDetector.kt
@@ -22,14 +22,11 @@ class HybridImageFaceDetector(
   )
 
   private fun resolveInputImage(
-    input: Any?
+    input: InputImage
   ): String {
     return when (input) {
-      is String -> input
-      is Map<*, *> -> {
-        val uri = input["uri"] as? String
-        uri ?: throw IllegalArgumentException("Invalid image object: missing 'uri'")
-      }
+      is InputImage.First -> input.value
+      is InputImage.Third -> input.value.uri
       else -> throw IllegalArgumentException("Invalid image type. Expected string or { uri }")
     }
   }
diff --git a/node_modules/react-native-vision-camera-face-detector/ios/Extensions/ML+FaceDetectorOptions.swift b/node_modules/react-native-vision-camera-face-detector/ios/Extensions/ML+FaceDetectorOptions.swift
index 518612a..6f2ba54 100644
--- a/node_modules/react-native-vision-camera-face-detector/ios/Extensions/ML+FaceDetectorOptions.swift
+++ b/node_modules/react-native-vision-camera-face-detector/ios/Extensions/ML+FaceDetectorOptions.swift
@@ -1,3 +1,4 @@
+#if !targetEnvironment(simulator)
 import MLKitFaceDetection
 
 extension FaceDetectorOptions {
@@ -75,3 +76,4 @@ private func buildMLFaceDetectorOptions(
 
   return options
 }
+#endif
diff --git a/node_modules/react-native-vision-camera-face-detector/ios/Extensions/ML+HybridFrameSpec.swift b/node_modules/react-native-vision-camera-face-detector/ios/Extensions/ML+HybridFrameSpec.swift
index 1672358..ad3fe5b 100644
--- a/node_modules/react-native-vision-camera-face-detector/ios/Extensions/ML+HybridFrameSpec.swift
+++ b/node_modules/react-native-vision-camera-face-detector/ios/Extensions/ML+HybridFrameSpec.swift
@@ -1,3 +1,4 @@
+#if !targetEnvironment(simulator)
 import MLKitVision
 import NitroModules
 import VisionCamera
@@ -23,3 +24,4 @@ extension HybridFrameSpec_protocol {
     return image
   }
 }
+#endif
diff --git a/node_modules/react-native-vision-camera-face-detector/ios/HybridFace.swift b/node_modules/react-native-vision-camera-face-detector/ios/HybridFace.swift
index f1d44fe..f4f56bb 100644
--- a/node_modules/react-native-vision-camera-face-detector/ios/HybridFace.swift
+++ b/node_modules/react-native-vision-camera-face-detector/ios/HybridFace.swift
@@ -1,3 +1,4 @@
+#if !targetEnvironment(simulator)
 import MLKitFaceDetection
 import NitroModules
 import Foundation
@@ -208,3 +209,4 @@ final class HybridFace: HybridFaceSpec {
     return config.height
   }
 }
+#endif
diff --git a/node_modules/react-native-vision-camera-face-detector/ios/HybridFaceDetector.swift b/node_modules/react-native-vision-camera-face-detector/ios/HybridFaceDetector.swift
index afa7c9b..47bc957 100644
--- a/node_modules/react-native-vision-camera-face-detector/ios/HybridFaceDetector.swift
+++ b/node_modules/react-native-vision-camera-face-detector/ios/HybridFaceDetector.swift
@@ -1,3 +1,4 @@
+#if !targetEnvironment(simulator)
 import MLKitFaceDetection
 import MLKitVision
 import NitroModules
@@ -67,3 +68,4 @@ class HybridFaceDetector: HybridFaceDetectorSpec {
     }
   }
 }
+#endif
diff --git a/node_modules/react-native-vision-camera-face-detector/ios/HybridFaceDetectorOutput.swift b/node_modules/react-native-vision-camera-face-detector/ios/HybridFaceDetectorOutput.swift
index 83e8cf5..84968c0 100644
--- a/node_modules/react-native-vision-camera-face-detector/ios/HybridFaceDetectorOutput.swift
+++ b/node_modules/react-native-vision-camera-face-detector/ios/HybridFaceDetectorOutput.swift
@@ -1,3 +1,4 @@
+#if !targetEnvironment(simulator)
 import AVFoundation
 import MLKitFaceDetection
 import MLKitVision
@@ -124,3 +125,4 @@ NativeCameraOutput {
     connection.preferredVideoStabilizationMode = .off
   }
 }
+#endif
diff --git a/node_modules/react-native-vision-camera-face-detector/ios/HybridImageFaceDetector.swift b/node_modules/react-native-vision-camera-face-detector/ios/HybridImageFaceDetector.swift
index 608050c..d12fe4b 100644
--- a/node_modules/react-native-vision-camera-face-detector/ios/HybridImageFaceDetector.swift
+++ b/node_modules/react-native-vision-camera-face-detector/ios/HybridImageFaceDetector.swift
@@ -1,3 +1,4 @@
+#if !targetEnvironment(simulator)
 import Foundation
 import MLKitFaceDetection
 import MLKitVision
@@ -52,15 +53,15 @@ class HybridImageFaceDetector: HybridImageFaceDetectorSpec {
   }
 
   private func resolveInputImage(
-    _ input: Any
+    _ input: InputImage
   ) throws -> String {
-    if let uri = input as? String {
-      return uri
-    }
-
-    if let object = input as? [String: Any],
-      let uri = object["uri"] as? String {
+    switch input {
+    case .first(let uri):
       return uri
+    case .third(let object):
+      return object.uri
+    case .second:
+      break
     }
 
     throw NSError(
@@ -122,3 +123,4 @@ class HybridImageFaceDetector: HybridImageFaceDetectorSpec {
     return image
   }
 }
+#endif
diff --git a/node_modules/react-native-vision-camera-face-detector/ios/SimulatorStubs.swift b/node_modules/react-native-vision-camera-face-detector/ios/SimulatorStubs.swift
new file mode 100644
index 0000000..1e1a16c
--- /dev/null
+++ b/node_modules/react-native-vision-camera-face-detector/ios/SimulatorStubs.swift
@@ -0,0 +1,51 @@
+#if targetEnvironment(simulator)
+import AVFoundation
+import NitroModules
+import VisionCamera
+
+class HybridFaceDetector: HybridFaceDetectorSpec {
+  init(options: FaceDetectorOptions) {
+    super.init()
+  }
+
+  func detectFaces(frame: any HybridFrameSpec) throws -> [any HybridFaceSpec] {
+    return []
+  }
+}
+
+class HybridImageFaceDetector: HybridImageFaceDetectorSpec {
+  init(options: ImageFaceDetectorOptions) {
+    super.init()
+  }
+
+  func detectFaces(image: InputImage) throws -> [any HybridFaceSpec] {
+    return []
+  }
+}
+
+class HybridFaceDetectorOutput: HybridCameraOutputSpec, NativeCameraOutput {
+  let output: AVCaptureVideoDataOutput
+  let requiresAudioInput: Bool = false
+  let requiresDepthFormat: Bool = false
+  let mediaType: MediaType = .video
+  let streamType: StreamType = .video
+  var outputOrientation: CameraOrientation = .up
+  var currentResolution: Size? { nil }
+  var targetResolution: ResolutionRule {
+    .closestTo(Size(width: 720.0, height: 1280.0))
+  }
+
+  init(options: FaceDetectorOutputOptions) {
+    self.output = AVCaptureVideoDataOutput()
+    super.init()
+    self.output.alwaysDiscardsLateVideoFrames = true
+  }
+
+  func configure(config: OutputConfiguration) {
+    guard let connection = self.output.connection(with: .video) else {
+      return
+    }
+    connection.preferredVideoStabilizationMode = .off
+  }
+}
+#endif
